import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    borderRadius: 24,
    display: 'flex',
    flexDirection: 'row',
    width: '90%',
    marginLeft: 20,
    marginBottom: 8,
  },
  image: {
    width: 100,
    height: 100,
    borderRadius: 24,
  },
});

export default styles;
