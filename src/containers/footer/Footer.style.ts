import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  card: {
    display: 'flex',
    marginHorizontal: 16,
    borderRadius: 10,
    marginBottom: 8,
    paddingHorizontal: 16,
  },
});
